apiVersion: v1
kind: Service
metadata:
  name: obp-mock-svc-{{ ns_name }}
 # annotations:
  #  service.beta.kubernetes.io/aws-load-balancer-internal: '"true"'
  labels:
    app: obp-mock-{{ ns_name }}
spec:
  ports:
  - port: 80
    name: http
    targetPort: 80
  selector:
    app: obp-mock-{{ ns_name }}
#  type: LoadBalancer
