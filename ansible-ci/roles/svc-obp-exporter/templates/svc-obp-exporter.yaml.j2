apiVersion: v1
kind: Service
metadata:
  name: obp-exporter-svs-{{ ns_name }}
 # annotations:
  #  service.beta.kubernetes.io/aws-load-balancer-internal: '"true"'
  labels:
    app: obp-exporter-{{ ns_name }}
spec:
  ports:
  - port: 80
    name: http
    targetPort: 80
  selector:
    app: obp-exporter-{{ ns_name }}
#  type: LoadBalancer 
