apiVersion: v1
kind: Service
metadata:
  name: obp-exporter-service
 # annotations:
  #  service.beta.kubernetes.io/aws-load-balancer-internal: '"true"'
  labels:
    app: obp-exporter 
spec:
  ports:
  - port: 80
    name: http
    targetPort: 80
  selector:
    app: obp-exporter
#  type: LoadBalancer  
   